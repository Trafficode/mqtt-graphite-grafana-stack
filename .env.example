# ============================================================================
# .env.example - REFERENCE TEMPLATE ONLY
# ============================================================================
# This file is NOT used by the setup process.
# It's kept as documentation to show what variables are used.
#
# ACTUAL WORKFLOW:
# 1. Edit config.json with your settings
# 2. Run ./scripts/setup.sh
# 3. .env is auto-generated from config.json (DO NOT edit .env manually)
#
# This .env.example file is just for reference.
# ============================================================================

# MQTT Configuration
MQTT_BROKER=mqtt.example.com
MQTT_PORT=1883
# Topic prefix (path before /UID/data)
# Example: "sensors/home" results in subscription to "sensors/home/+/data"
MQTT_TOPIC_PREFIX=sensors/home
# Topic pattern after prefix (usually "+/data" for all sensors)
MQTT_TOPIC=+/data
MQTT_USERNAME=
MQTT_PASSWORD=

# Graphite Configuration
GRAPHITE_HOST=graphite
GRAPHITE_PORT=2003
GRAPHITE_TIME_ZONE=Europe/Warsaw

# Grafana Configuration
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=change_this_password

# Logging
LOG_LEVEL=INFO
